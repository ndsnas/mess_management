
					<%= @savedornot %>
					<%= form_for @credentials, url: {action: "change_password"} do |f| %>
					<table >
					  <tr>
					    <th style="text-align: left">Old password</th>
					    <td><%= f.text_field(:admin) %></td>
					  </tr>
					  <tr>
					    <th style="text-align: left">New Password</th>
					    <td><%= f.text_field(:password) %></td>
					  </tr>
					</table>
					      <%= f.submit("Change") %>
					<% end %>
