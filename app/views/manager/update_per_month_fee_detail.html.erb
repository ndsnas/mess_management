<%=form_for @bill, url: {action: "update_per_month_fee_detail"} do |f| %>
<table summary="Subject form fields">
  <tr>
    <th>Status</th>
    <td><%= f.select :status, [['unpaid', 0], ['paid', 1]] %></td>
  </tr>
  <td><%= f.submit("Update") %></td>


<% end %>
